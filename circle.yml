checkout:
  pre:
    - git config user.email determinator@deliveroo.co.uk
    - git config user.name "Determinator CI"

deployment:
  release:
    branch: master
    commands:
      - bundle exec rake version:bump release
